// Interactive Button:
// Create a button. When clicked, it should display an alert with the message
// "Button was clicked!".
const button = document.querySelector("button");

button.addEventListener("click", function () {
  prompt("Arrrest Him", "Button was clicked!");
});
